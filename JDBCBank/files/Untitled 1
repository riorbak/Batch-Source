Database Structure

/*
CREATE TABLE BankUsers 
(
    UserID NUMBER NOT NULL PRIMARY KEY,
    RealName varchar(255),
    PhoneNum varchar(255),
    
    UserName varchar(255) NOT NULL,
    UserPwd varchar(255) NOT NULL,
    AccessLevel NUMBER(1) CHECK (AccessLevel=0 OR AccessLevel=1)
);

CREATE TABLE Accounts
(
    Acc_Num NUMBER NOT NULL PRIMARY KEY,    
    Acc_Bal NUMBER(8,2),

    UserID NUMBER NOT NULL,
    Acc_Active  NUMBER(1) CHECK (Acc_Active = 0 OR Acc_Active = 1),
    is_Joint NUMBER(1) CHECK (is_Joint = 0 OR is_Joint = 1),
    JointUserID NUMBER
);
*/